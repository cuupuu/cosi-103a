{
  "devDependencies": {
    "@testing-library/dom": "^9.3.4",
    "@testing-library/react":"14.2.1",
    "@testing-library/user-event": "^12.8.3",
    "react-scripts": "4.0.3"
  },
  "scripts": {
    "test": "react-scripts test --env=jsdom" // add --env=jsdom to use
  }
}
